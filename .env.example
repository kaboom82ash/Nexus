# Optional: enable LIVE Gmail data.
# Create a Web-application OAuth Client ID in Google Cloud Console with the
# gmail.readonly scope, and add this site's origin to "Authorized JavaScript
# origins". Leave unset to run the dashboard with sample data.
VITE_GOOGLE_CLIENT_ID=
